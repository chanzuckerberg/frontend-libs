{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Unlike the other packages, we don't build this one's TypeScript. But we still need
    // TypeScript to analyze its .ts files.
    "noEmit": true,
    "jsx": "react-jsx"
  }
}
